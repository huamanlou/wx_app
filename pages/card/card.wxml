<!--card.wxml-->
<block wx:for="{{cards}}">
  <image class="card-pic" src="{{item.pic}}"  mode="aspectFit" bindtap="listClick" id="{{item.id}}"></image>
  <view>{{item.name}}</view>
  <view>{{item.desc}}</view>
</block>
